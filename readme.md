Project 2: a Code Snippet Library API. Developers often find themselves searching for the same pieces of code over and over (like a specific database connection setup or a helper function). This API allows you to save these snippets, tag them by language, and retrieve them later.

Clone the repo → git clone YOUR_URL cd into it → npm install Create/refresh .env with MONGODB_URI + PORT npm start
